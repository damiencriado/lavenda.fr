# lavenda

## Aperçu local (Docker)

Le projet est un site statique servi par Nginx.

```bash
docker compose up --build
```

Puis ouvre:
- http://localhost:8090/

### Arrêter

```bash
docker compose down
```
